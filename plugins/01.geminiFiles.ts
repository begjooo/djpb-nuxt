export default defineNuxtPlugin(async () => {
  // console.log(`geminiFiles plugins`);
  // const geminiFiles: DriveFile = useState('gemini-files', () => []);
  // const { data } = await useFetch(`/api/gemini-files`);
  // geminiFiles.value = data.value;
});