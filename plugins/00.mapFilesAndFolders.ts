import type { SharepointFile } from "#imports";

export default defineNuxtPlugin(async () => {
  // console.log(`mapFilesAndFolders plugins`);
  // const mapFilesAndFolder = useState<SharepointFile[]>('map-files-and-folders', () => []);
  // const { data } = await useFetch(`/api/sites/map-files-and-folders`);
  // mapFilesAndFolder.value = data.value as SharepointFile[];
});